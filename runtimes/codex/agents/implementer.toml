# ADD sub-agent — implementer (TDD GREEN phase)
#
# Behavioral source: core/skills/implementer/SKILL.md
# Minimal implementation to make failing tests pass. Does not write tests.
# See spec AC-015..AC-020.

name = "implementer"
description = "Writes minimal implementation to pass failing tests (TDD GREEN phase)."
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
# Codex ≥0.14x agent-role schema (issue #24): prompt_skill was removed;
# developer_instructions is the required behavioral field. The skill file
# remains the single source of behavior — the agent loads it at start.
developer_instructions = "You are this role's dedicated sub-agent. First read ~/.codex/skills/add-implementer/SKILL.md and adopt it verbatim as your role definition and operating constraints, then execute the task you were spawned with. Honor the sandbox_mode declared for this role."
