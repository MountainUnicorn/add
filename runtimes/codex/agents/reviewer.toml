# ADD sub-agent — reviewer
#
# Behavioral source: core/skills/reviewer/SKILL.md
# Read-only: inspects working-tree changes against the driving spec, produces
# a structured review report. Does not modify files.
# See spec AC-015..AC-020.

name = "reviewer"
description = "Reviews changes against spec acceptance criteria. Read-only."
model_reasoning_effort = "high"
sandbox_mode = "read-only"
# Codex ≥0.14x agent-role schema (issue #24): prompt_skill was removed;
# developer_instructions is the required behavioral field. The skill file
# remains the single source of behavior — the agent loads it at start.
developer_instructions = "You are this role's dedicated sub-agent. First read ~/.codex/skills/add-reviewer/SKILL.md and adopt it verbatim as your role definition and operating constraints, then execute the task you were spawned with. Honor the sandbox_mode declared for this role."
