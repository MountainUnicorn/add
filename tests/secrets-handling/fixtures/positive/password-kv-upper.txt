# Positive fixture: PASSWORD_KV — uppercase regression (v0.9.10).
# The catalog regex is case-insensitive via character classes; PASSWORD=
# must match. Test script synthesizes the match string at runtime.
settings = {
  <SYNTHESIZED:PASSWORD_KV_UPPER>
}
