# Positive fixture: PASSWORD_KV
# Matches catalog regex (case-insensitive via char classes):
#   [Pp][Aa][Ss][Ss][Ww][Oo][Rr][Dd][[:space:]]*[:=]...
# Test script synthesizes the match string at runtime.
db_config = {
  <SYNTHESIZED:PASSWORD_KV>
}
