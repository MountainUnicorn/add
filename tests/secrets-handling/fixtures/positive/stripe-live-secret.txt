# Positive fixture: STRIPE_LIVE_SECRET
# Matches regex: (sk|pk)_live_[A-Za-z0-9]{24,}
# The test script synthesizes the actual match string at runtime to avoid
# tripping GitHub Advanced Security push protection on what looks like a
# real Stripe key. See tests/secrets-handling/test-secrets-handling.sh §
# synthesize_positive.
STRIPE_SECRET=<SYNTHESIZED:STRIPE_LIVE_SECRET>
