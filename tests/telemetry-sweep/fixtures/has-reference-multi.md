---
description: "[ADD v0.0.0] Fixture skill that already had references and appended telemetry"
allowed-tools: [Read]
references: ["learning-reference.md", "swarm-protocol.md", "rules/telemetry.md"]
---

# Fixture: skill with multiple references including telemetry

This fixture mirrors the v0.7.x skills (`cycle`, `verify`, etc.) that
already declared `references:` for other reasons and now have
`rules/telemetry.md` appended.
