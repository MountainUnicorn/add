---
description: "[ADD v0.0.0] Fixture skill with no references key at all"
allowed-tools: [Read]
---

# Fixture: skill with no references key

This fixture must FAIL the coverage test — there is no `references:` key
at all, so `rules/telemetry.md` cannot be present.
